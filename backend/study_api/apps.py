from django.apps import AppConfig


class StudyApiConfig(AppConfig):
    """Django application configuration for the study API app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "study_api"
